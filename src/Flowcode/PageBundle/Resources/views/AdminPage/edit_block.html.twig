{% extends '::backend.html.twig' %}

{% block body -%}
    <h1>Block edit</h1>

    {{ form(edit_form, { 'style':'horizontal'}) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_page_blocks', { 'page_id':entity.getPage.id }) }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
