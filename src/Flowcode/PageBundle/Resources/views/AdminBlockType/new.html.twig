{% extends '::backend.html.twig' %}

{% block body -%}
    <h1>BlockType creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('blocktype') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
