{% extends '::backend.html.twig' %}

{% block body -%}
<div class="page-header">
    <h1>Project edit</h1>
    <ul class="record_actions list-inline">
        <li>
            <a href="{{ path('admin_project_list') }}" class="btn btn-default">
                Back to the list
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
</div>
    {{ form(edit_form, { 'style':'horizontal' }) }}

{% endblock %}
